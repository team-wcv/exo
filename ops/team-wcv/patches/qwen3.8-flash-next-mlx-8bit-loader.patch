diff --git a/qwen4_exp.py b/qwen4_exp.py
--- a/qwen4_exp.py
+++ b/qwen4_exp.py
@@ -166,6 +166,14 @@ class RotaryEmbedding(nn.Module):
         return mx.cos(emb), mx.sin(emb)
 
 
+def _cache_positions(offset, length: int) -> mx.array:
+    """Build per-request positions for scalar and batched MLX cache offsets."""
+    steps = mx.arange(length)[None, :]
+    if isinstance(offset, mx.array):
+        return offset.reshape(-1, 1) + steps
+    return steps + offset
+
+
 # ------------------------------------------------------------------------ QSA
 
 
@@ -219,8 +227,8 @@ class Qwen4ExpQSA(nn.Module):
         cos_k, sin_k = rope(block_starts[None, :])
         pooled = _rope_partial(pooled, cos_k, sin_k)
 
-        q_pos = mx.arange(offset, offset + S)
-        cos_q, sin_q = rope(q_pos[None, :])
+        q_pos = _cache_positions(offset, S)
+        cos_q, sin_q = rope(q_pos)
         q = self.q_layernorm(q)
         q = _rope_partial(q, cos_q[:, :, None, :], sin_q[:, :, None, :])
 
@@ -232,7 +240,7 @@ class Qwen4ExpQSA(nn.Module):
 
         # un bloc n'est candidat que s'il est entièrement dans le passé de la requête
         block_end = block_starts + self.compress_ratio - 1
-        visible = block_end[None, None, :] <= q_pos[None, :, None]
+        visible = block_end[None, None, :] <= q_pos[:, :, None]
         scores = mx.where(visible, scores, -mx.inf)
 
         k = min(self.block_topk, n_blocks)
@@ -254,7 +262,7 @@ class Qwen4ExpQSA(nn.Module):
                 [keep, mx.zeros((B, S, tail), dtype=mx.bool_)], axis=-1
             )
         key_pos = mx.arange(kv_len)[None, None, :]
-        qp = q_pos[None, :, None]
+        qp = q_pos[:, :, None]
         own_block_start = ((qp + 1) // self.compress_ratio) * self.compress_ratio
         own_tail = (key_pos >= own_block_start) & (key_pos <= qp)
         keep = (keep | own_tail) & (key_pos <= qp)
@@ -292,7 +300,7 @@ class Qwen4ExpAttention(nn.Module):
         )
         v = self.v_proj(x).reshape(B, S, self.n_kv_heads, -1).transpose(0, 2, 1, 3)
 
-        cos, sin = rope(mx.arange(offset, offset + S)[None])
+        cos, sin = rope(_cache_positions(offset, S))
         cos, sin = cos[:, None], sin[:, None]
         q, k = _rope_partial(q, cos, sin), _rope_partial(k, cos, sin)
 
@@ -738,11 +746,14 @@ class Qwen4ExpTextModel(nn.Module):
         conv_mask = None
 
         prev_ctx = None
-        if self.ple_layers:
+        # Exo pipeline sharding replaces ``self.layers`` after construction.
+        # Derive PLE positions from the live slice instead of the original config.
+        ple_layers = [i for i, layer in enumerate(self.layers) if layer.ple is not None]
+        if ple_layers:
             ctx_len = self.args.ngram_size - 1
             eos = self.args.eos_token_id
             eos = eos[0] if isinstance(eos, list) else eos
-            pc = cache[self.ple_layers[0]]
+            pc = cache[ple_layers[0]]
             prev = pc[3] if pc is not None else None
             prev_ctx = (
                 prev
@@ -808,7 +819,9 @@ class Model(nn.Module):
 
     def make_cache(self):
         caches = []
-        for i, t in enumerate(self.args.text.layer_types):
+        # Exo pipeline sharding replaces the layer list after construction.
+        for layer in self.model.layers:
+            t = layer.layer_type
             if t == "full_attention":
                 caches.append(_AttnCache())
             else:
